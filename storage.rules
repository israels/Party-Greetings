rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public uploads to greetings folder
    match /greetings/{allPaths=**} {
      allow read, write;
    }
    
    // Block everything else
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}